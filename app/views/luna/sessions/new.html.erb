<h2><%= t 'sessions.new.title' %></h2>

<%= form_for(:user, as: :user, url: open_session_path, method: :post) do |f| %>

  <%= f.label :email %>
  <%= f.email_field :email %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit :login %>

<% end %>

